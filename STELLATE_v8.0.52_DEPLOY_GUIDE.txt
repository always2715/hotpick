STELLATE v8.0.52 배포 및 단계 제한 중단 실행 복구 안내

1. STELLATE_v8.0.52_REPLACE_ONLY.zip을 별도 폴더에 압축 해제합니다.
2. 내부 파일과 폴더 전체를 기존 hotpick 저장소 최상위에 덮어씁니다.
3. APPLY_STELLATE_v8.0.52_CLEANUP.bat을 실행합니다.
4. GitHub Desktop에서 변경·삭제 파일 전체를 커밋하고 Push합니다.
5. Vercel 배포 후 /api/version에서 확인합니다.
   - appVersion: 8.0.52
   - contentVersion: 138
   - trendCacheVersion: 55
   - automaticRunStepBudget: 96
   - manualRunStepBudget: 160
   - hardMaxRunSteps: 240
   - maxStalledRunSteps: 10
6. 관리자에서 trend_refresh_step_limit으로 중단된 실행의 '중단 지점부터 재개'를 실행합니다.
7. 관리자 화면에서 실행 단계가 current/budget 형태로 증가하는지 확인합니다.
8. 완료된 후보가 다시 생성되지 않고 미완료 작업 또는 finalize부터 이어지는지 확인합니다.

정책:
- 실제 진행이 있으면 단계 예산을 24씩 최대 240까지 확장
- 동일 진행 상태가 10회 반복될 때만 무한 반복으로 중단
- 재개 시 새 120분 실행 시간창과 QStash queueGeneration 적용
- TOP25 선정, 혼합 TOP20 공개, 누적 피드, 500개 썸네일 정책은 변경하지 않음

금지:
- Redis FLUSHALL
- 기존 정상 TOP 삭제
- 완료된 stage·snapshot 삭제

권장 커밋:
fix: use progress-aware TOP refresh step budget v8.0.52
