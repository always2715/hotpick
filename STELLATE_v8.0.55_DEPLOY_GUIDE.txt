STELLATE v8.0.55 배포 및 운영 확인

1. STELLATE_v8.0.55_FULL.zip 또는 STELLATE_v8.0.55_REPLACE_ONLY.zip을 별도 폴더에 압축 해제합니다.
2. 내부 파일·폴더 전체를 hotpick 저장소 최상위에 덮어씁니다.
3. APPLY_STELLATE_v8.0.55_CLEANUP.bat을 실행합니다.
4. GitHub Desktop에서 변경·삭제 전체를 확인합니다.
5. 커밋 메시지 권장: fix: recover TOP20 generation and carryover v8.0.55
6. Commit to main 후 Push origin 합니다.
7. Vercel 배포가 Ready인지 확인합니다.
8. /api/version에서 다음 값을 확인합니다.
   - appVersion 8.0.55
   - contentVersion 139
   - trendCacheVersion 56
9. 기존 공개 TOP과 Redis 데이터를 삭제하지 않습니다.
10. 관리자에서 누적 피드 전체 재구성을 1회 실행해 archiveCount를 확인합니다.
11. 보고된 기존 2/20 실패 실행은 재개하지 말고 새 TOP 작업을 시작합니다.
12. 새 실행에서 아래를 확인합니다.
   - 반도체, 가스 누출, 구조팀 같은 포괄 후보 제외
   - 시도 후보별 stage·snapshot·candidate/slug/stage alias 존재
   - 본문 미달 시 targeted_fact_rewrite 표시
   - 실제 오류 코드와 KEYWORD_ATTEMPT_LIMIT 별도 표시
   - 신규 15~19개일 때 마지막 정상 archive carryover 동작
   - 정상 20개 확보 시 낮은 순위 SKIPPED_NOT_NEEDED
13. Redis FLUSHALL, 피드 초기화, 기존 TOP 삭제는 금지합니다.
14. npm audit fix --force는 실행하지 않습니다.
