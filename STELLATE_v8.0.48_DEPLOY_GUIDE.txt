STELLATE v8.0.48 배포 및 실패 실행 복구 안내
작성일: 2026-06-25

1. 적용 파일
- 권장: STELLATE_v8.0.48_REPLACE_ONLY.zip
- 전체 복구·검토: STELLATE_v8.0.48_FULL.zip

2. GitHub Desktop 적용
1) Fetch origin 및 필요 시 Pull origin
2) 교체 ZIP을 저장소 밖 별도 폴더에 압축 해제
3) ZIP 내부 전체를 hotpick 저장소 최상위에 덮어쓰기
4) APPLY_STELLATE_v8.0.48_CLEANUP.bat 실행
5) GitHub Desktop에서 변경과 삭제를 모두 확인
6) node_modules, .next, .env, .env.local, .vercel은 커밋하지 않음
7) Commit to main
8) Push origin

권장 커밋:
fix: recover insufficient evidence candidates and rebuild top25 pool v8.0.48

3. 배포 확인
/api/version에서 다음을 확인:
- appVersion: 8.0.48
- contentVersion: 137
- trendCacheVersion: 54
- generationPoolCount: 25
- publicTopCount: 20
- thumbnailPoolSize: 500

4. 기존 16/25 실패 실행
기존 실행에는 문장 조각 후보가 이미 고정돼 있으므로 재개하지 않습니다.

필수 순서:
1) 관리자 로그인
2) 기존 실행을 `현재 TOP 작업 중단` 처리
3) `TOP 즉시 시작`으로 새 실행 생성
4) 새 후보 목록에 문장 조각이 없는지 확인
5) 성공 후보 20개 이상이면 공개 확인

관리자에서 과거 실행 재개를 시도해도 문장 조각 후보가 있으면 candidatePoolRebuildRequired로 차단됩니다.

5. 정상 확인 예
차단돼야 하는 후보:
- 이기려고 한
- 기자수첩 인천시장직 인수위는 심판자
- 관형형·서술형으로 끝난 미완결 기사 문구

재시도돼야 하는 오류:
- INSUFFICIENT_KEYWORD_EVIDENCE
- 키워드의 정체 또는 현재 이슈를 설명할 확인 자료 부족

확인할 저장 상태:
- researchCheckpoint
- publication stage
- run snapshot
- candidate alias
- slug alias

6. 하지 말아야 할 작업
- Redis FLUSHALL
- 기존 정상 TOP20 삭제
- 누적 피드 초기화
- 기존 실패 실행 강제 재개
- 후보 문장 조각 관리자 승인
- ZIP 일부 파일만 선택 덮어쓰기

7. 운영 검증 한계
로컬 전체 테스트와 프로덕션 빌드는 통과했습니다.
운영 Upstash Redis·QStash 및 외부 검색 API를 사용한 실제 TOP25 전체 실행은 배포 후 관리자 화면에서 확인해야 합니다.
