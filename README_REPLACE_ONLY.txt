STELLATE v8.0.42 GitHub Desktop replacement package

Copy every file and folder from this directory into the hotpick repository root and overwrite existing files. Commit all changed and new files, then push.

If foreign app/ or .next files exist, run APPLY_STELLATE_v8.0.42_CLEANUP.bat before commit.

After deployment confirm /api/version = 8.0.42, open /feed, and check Admin > Thumbnail images.
